# Multi-Agent Workflow Threat Model

## Key threats
Trust propagation failures, unclear authority, cascading errors, insecure inter-agent messages, spoofed agent identity.

## Controls
Authenticated channels, scoped delegation, signed messages where feasible, per-agent logs, circuit breakers.

